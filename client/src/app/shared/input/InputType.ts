export enum InputType {
  color = 'color',
  date = 'date',
  datetime_local = 'datetime-local',
  email = 'email',
  month = 'month',
  // eslint-disable-next-line
  number = 'number',
  // eslint-disable-next-line
  integer = 'number',
  // eslint-disable-next-line
  decimal = 'number',
  password = 'password',
  search = 'search',
  tel = 'tel',
  text = 'text',
  // eslint-disable-next-line
  string = 'text',
  time = 'time',
  url = 'url',
  week = 'week',
}
