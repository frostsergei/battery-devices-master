import { PreventDefaultStopPropagationDirective } from './prevent-default-stop-propagation.directive';

describe('PreventDefaultStopPropagationDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventDefaultStopPropagationDirective();
    expect(directive).toBeTruthy();
  });
});
